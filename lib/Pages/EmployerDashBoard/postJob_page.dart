import 'package:flutter/material.dart';

class PostJobPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("PostPage"),),
    );
  }
}