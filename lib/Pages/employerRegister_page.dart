import 'package:flutter/material.dart';

class EmployerRegisterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Employer Register Page"),),
    );
  }
}