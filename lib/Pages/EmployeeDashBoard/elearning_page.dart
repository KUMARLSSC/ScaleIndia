import 'package:flutter/material.dart';

class ElearningPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ElearningPage'),
    );
  }
}
